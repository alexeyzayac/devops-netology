server {
    listen {{ lighthouse_nginx_port }};
    server_name {{ lighthouse_server_name }};

    root {{ lighthouse_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}